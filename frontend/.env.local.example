# Server-only origins used by Next.js rewrites. These values are never exposed
# to browser bundles. They are read when Next.js starts in development and when
# `npm run build` runs for production. See PRODUCTION.md.
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
AGENT_INTERNAL_URL=http://127.0.0.1:4173
